// external modules
import React, { FC } from 'react'

// types
interface HeaderProps {
 
}

const Header: FC<HeaderProps> = ({}) => {
  return (
    <header>
        
    </header>
	);
};

export default Header;